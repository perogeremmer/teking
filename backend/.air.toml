root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/ayomendaki-admin . && ./tmp/ayomendaki-admin --dev"
  bin = "./tmp/ayomendaki-admin"
  include_ext = ["go", "html"]
  exclude_dir = ["tmp", "data", "static"]
  exclude_regex = ["_test.go"]
  stop_on_error = false
  args_bin = ["--dev"]
